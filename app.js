const STATES={AL:.041,AK:0,AZ:.056,AR:.065,CA:.073,CO:.029,CT:.063,DE:0,DC:.06,FL:.06,GA:.04,HI:.04,ID:.06,IL:.063,IN:.07,IA:.06,KS:.065,KY:.06,LA:.045,ME:.055,MD:.06,MA:.0625,MI:.06,MN:.0688,MS:.07,MO:.0425,MT:0,NE:.0555,NV:.0685,NH:0,NJ:.0663,NM:.0513,NY:.08,NC:.0475,ND:.05,OH:.0575,OK:.045,OR:0,PA:.06,RI:.07,SC:.06,SD:.045,TN:.07,TX:.0625,UT:.061,Vt:.066,VA:.053,WA:.065,WV:.06,WI:.0575,WY:.04};
const PLATFORM_SETS={all:{eBay:[.1325,.30],Poshmark:[.20,0],Mercari:[.10,0],Etsy:[.095,.45]},mercari:{Mercari:[.10,0]},depop:{Depop:[.10,0]},compare:{Poshmark:[.20,0],eBay:[.1325,.30]}};
const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n);
function inputs(){return ['salePrice','itemCost','sellerShipping','buyerShipping'].map(id=>+document.getElementById(id)?.value||0)}
function stateTax(){const state=document.getElementById('state')?.value||'CA';return STATES[state]??0}
function card(name,fee,net,profit,margin,shareText){const el=document.createElement('article');el.className='result-card';el.innerHTML=`<div class="result-card__top"><div><span class="eyebrow">${name}</span><h3>${money(profit)} <small>profit</small></h3></div><span class="badge">${margin.toFixed(1)}% margin</span></div><dl><div><dt>Estimated fees</dt><dd>${money(fee)}</dd></div><div><dt>Net payout</dt><dd>${money(net)}</dd></div></dl><div class="card-actions"><button class="button button--secondary copy-btn" type="button">Copy</button><button class="button button--primary share-btn" type="button">Share</button></div>`;el.querySelector('.copy-btn').onclick=async()=>{await navigator.clipboard?.writeText(shareText);el.querySelector('.copy-btn').textContent='Copied!';setTimeout(()=>el.querySelector('.copy-btn').textContent='Copy',1400)};el.querySelector('.share-btn').onclick=async()=>{if(navigator.share) await navigator.share({title:`${name} reseller estimate`,text:shareText,url:location.href});else{await navigator.clipboard?.writeText(shareText);el.querySelector('.share-btn').textContent='Link copied!';setTimeout(()=>el.querySelector('.share-btn').textContent='Share',1400)}};return el}
function calculate(){const [price,cost,shipping,buyerShipping]=inputs(),results=document.getElementById('results');if(!results)return;results.innerHTML='';const set=PLATFORM_SETS[document.body.dataset.platform||'all'];const estimates=[];for(const [name,[rate,flat]] of Object.entries(set)){const fee=price*rate+flat,net=price-fee-shipping+buyerShipping,profit=net-cost,margin=net?profit/net*100:0;const text=`${name}: ${money(profit)} estimated profit, ${money(net)} net payout, ${margin.toFixed(1)}% margin.`;estimates.push({name,profit,fee,net,margin});results.appendChild(card(name,fee,net,profit,margin,text))}const best=estimates.sort((a,b)=>b.profit-a.profit)[0];const recommendation=document.getElementById('recommendation');if(recommendation)recommendation.innerHTML=price?`<strong>${best.name} looks best for this sale.</strong> Based on your inputs, it estimates ${money(best.profit)} profit after marketplace fees, shipping, and item cost. Compare current seller policies before listing.`:'Enter your sale details to get a personalized marketplace recommendation.';const tax=document.getElementById('taxEstimate');if(tax)tax.textContent=`Estimated buyer sales tax in ${document.getElementById('state')?.value||'CA'}: ${money(price*stateTax())} (${(stateTax()*100).toFixed(2)}%). Sales tax rules vary and this is not a seller-tax calculation.`}
document.addEventListener('DOMContentLoaded',()=>{const select=document.getElementById('state');if(select)Object.keys(STATES).sort().forEach(s=>{const o=document.createElement('option');o.value=s;o.textContent=s;select.appendChild(o)});document.querySelectorAll('input,select').forEach(i=>i.addEventListener('input',calculate));calculate()});
